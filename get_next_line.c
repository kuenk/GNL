



#include "get_next_line.h"

char    *get_next_line(int fd)
{
    static char 
    char    *

     = read_buffer(fd, );
     if( == NULL)
     {
        free();
        = NULL,
        return(NULL);

     }

}